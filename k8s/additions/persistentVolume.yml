apiVersion: v1
kind: PersistentVolume
metadata:
  name: salinesel-in
spec:
  storageClassName: standard-rwo
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  claimRef:
    namespace: salinesel-in
    name: salinesel-in
  nfs:
    path: /staticsite
    server: 10.20.8.66
